# Known Issues

Do not treat this file as an answer key.

The demo team has reported:

- backend health check passes
- recruiter login works
- candidate profiles load
- the recruiter workflow may still fail
- score/status/dashboard state may not be trustworthy
- some failures only appear after a specific candidate or action sequence

Your assigned incident packet contains the actual evidence.

Your job is to decide whether HireSignal is demo-ready by verifying the critical workflow, not by checking whether the app opens.
